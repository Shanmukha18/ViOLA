# Database Configuration (Supabase)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/viola_cab_sharing}
spring.datasource.host=${DATABASE_HOST:localhost}
spring.datasource.port=${DATABASE_PORT:5432}
spring.datasource.database=${DATABASE_NAME:viola_cab_sharing}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Shannu@2005}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration
jwt.secret=${JWT_SECRET:your-super-secret-jwt-key-here-make-it-very-long-and-secure}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:151964105245-nrj6fidkg102ub3blibqlb8ha9v25tcd.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# Server Configuration
server.port=${PORT:8081}
spring.application.name=viola-cab-sharing

# Logging (Production settings)
logging.level.com.viola=INFO
logging.level.org.springframework.security=WARN

# CORS Configuration (Production - will be updated with actual domains)
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}
